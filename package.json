{
  "name": "root",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build:app": "yarn --cwd app run build:cloud",
    "start": "yarn --cwd server && yarn --cwd server run start:cloud"
  },
  "engines": {
    "node": ">=12.0"
  }
}
